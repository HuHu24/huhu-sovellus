export function getImage(imageId = 's') {
    return (
        imageId +
        '.png'
    );
}
export function otsikko(id ? : string)  {
    return (
        'Ruoka peruttu'
    );
}
export function teksti(id ? : string) {
    return (
        'Joo safka ei oo hoitanut hommaansa niin kuollaan nälkään. Syy kissan karv...'
    );
}